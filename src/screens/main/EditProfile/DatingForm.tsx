import { ScrollView, View } from 'react-native';

const DatingForm = () => {
  return (
    <ScrollView>
      <View style={{ paddingVertical: 32 }}></View>
    </ScrollView>
  );
};
export default DatingForm;
